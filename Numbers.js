 const num1 =30;
 const num2 = 50;

       //  simple maths
 let val;
 val= num1+num2; //80
 val= num1-num2;  //-20
 val= num1*num2;  //1500
 val= num1/num2;  //0.6
 val= num1%num2;   //30

      //  math object
val=Math.PI;
val=Math.round(2.4);  //round off value- output
val=Math.ceil(2.1);   //3 - upper number
val=Math.floor(2.9);    //2 - lower number
val=Math.sqrt(64);  // 8
val=Math.abs(-5);    //absolute -positive output
val=Math.min(-5,5,2,1,0);  //minmum number
val=Math.max(45,32,22,11,8,789);
val=Math.random();   // while refreshing the brower ,we will get new number
val=Math.floor(Math.random()*10);




 console.log(val);
